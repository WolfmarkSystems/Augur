//! `verify` CLI — Sprint 1 scaffold.
//!
//! P4 wires the real `clap` subcommands (classify / transcribe /
//! translate) and `env_logger` so the binary has a structured
//! logging surface. For Sprint 1 we keep the binary compiling
//! with an empty `main`; the presence of a working workspace
//! build + a `verify` bin target is the P1 deliverable.

fn main() {}
